1. 튜플이란..?
 const tuple : [string, number]= ['kim',30];
 tuple[0] = 10;
 tuple[0] = 'hello';
 tuple.push(50);
 튜플(Tuples) 타입은 배열보다, 저장되는 요소의 순서와 수에 제약을 두고자 할 때 사용.

 - 배열은 순서상관없이 타입만 맞으면 개수상관없이 계속 넣을수 있지만,
   튜플은 수와 타입에 제약사항이 걸림

 - push함수로 요소를 추가할수는있음
